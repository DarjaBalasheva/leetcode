from style.base_style import colors

# Стили для фреймов

display_frame = {
    "height": 160,
    "bg": colors.get("first_color"),
}

button_frame = {
    "height": 150,
    "bg": colors.get("first_color"),
}
